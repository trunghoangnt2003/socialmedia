@{
    Layout = "_EmptyLayout";
}

@{
    ViewBag.Title = "Reset Password Successfully";
}

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg p-4 text-center" style="max-width: 400px; width: 100%;">
        <h2 class="text-success">Reset Password Successfully!</h2>
        <p>Let go to login to use system.</p>
        <a href="@Url.Action("Index", "Login")" class="btn btn-primary mt-3">Go to Login</a>
    </div>
</div>
